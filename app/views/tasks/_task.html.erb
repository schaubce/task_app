<div class="row">
  <div class="small-2 large-4 columns"><%= task.task_name %> </div>
  <!-- Here will actually be the progress bar--> 
  <div class="small-4 large-4 columns"><%= Category.find(task.category_id).name%></div>
  <div class="small-6 large-4 columns"><%= task.due_date %></div>
</div>
